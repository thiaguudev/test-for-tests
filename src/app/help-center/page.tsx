import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Survey Apoli - Help Center",
  description: "Help Center",
};

export default function HelpCenterPage() {
  return <div />;
}
